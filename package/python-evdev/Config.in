config BR2_PACKAGE_PYTHON_EVDEV
	bool "python-evdev"
	help
	
	  This package provides bindings to the generic input event
	  interface in Linux. The evdev interface serves the purpose
	  of passing events generated in the kernel directly to
	  userspace through character devices that are typically
	  located in /dev/input/.

	  https://github.com/gvalkov/python-evdev
